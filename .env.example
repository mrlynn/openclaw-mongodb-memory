# ============================================================
# OpenClaw Memory — Environment Configuration
# ============================================================
# Copy this file to .env.local and fill in the values:
#   cp .env.example .env.local
# ============================================================

# --- MongoDB ---
# Connection string for your MongoDB instance.
# Atlas:   mongodb+srv://<user>:<pass>@cluster.mongodb.net/
# Local:   mongodb://localhost:27017
MONGODB_URI=mongodb://localhost:27017

# --- Voyage AI Embeddings ---
# Get your key at https://dash.voyageai.com/
# Leave blank and set VOYAGE_MOCK=true for development without an API key.
VOYAGE_API_KEY=

# Set to "true" to use deterministic mock embeddings (no API calls).
# Great for local development and testing.
VOYAGE_MOCK=true

# Optional: custom Voyage endpoint (e.g. MongoDB Atlas AI endpoint)
# VOYAGE_BASE_URL=https://api.voyageai.com/v1

# Optional: Voyage model override (default: voyage-3)
# VOYAGE_MODEL=voyage-3

# --- Daemon ---
# Port the memory daemon listens on (default: 7654)
# MEMORY_DAEMON_PORT=7654

# Optional: API key to protect daemon endpoints.
# When set, all requests (except /health) must include X-API-Key header.
# MEMORY_API_KEY=

# --- Web Dashboard ---
# URL where the daemon is running (used by Next.js web UI)
NEXT_PUBLIC_DAEMON_URL=http://localhost:7654
